# 数据管道异常诊断报告

> **状态更新（2026-09-16 09:15）**：本报告的修复方案**已按"以 D 盘为主副本"的思路执行完毕**
> —— 数据合并、git 分叉合并、推送上线都已完成，线上数据已到 **2026-09-16**。
> 本文保留作为事故记录与根因分析。仍待用户操作的一项：以管理员身份运行
> `register_startup_admin.bat`，把两个计划任务从 E 盘改指向 D 盘。

- 诊断时间：2026-09-16 08:20 ~ 08:35
- 现象：线上看板（Streamlit Cloud）最新数据停留在 **2026-09-11**
- 结论：**不是源数据没到，也不是数据生成失败，而是「GitHub 推送自 9/11 起被持续拒绝」+「项目存在两份互相抢推的副本」**

---

## 一、诊断结论（一句话）

项目目录里存在**两份并存的 git 副本**（D 盘、E 盘）。9/11 上午两份副本同时对同一个
GitHub 仓库 `main` 分支推送，D 盘那一侧抢先推成功、占据了远程历史；E 盘那一侧从此
**每次 push 都被 `non-fast-forward` 拒绝**。E 盘仍在每天正常生成数据（已到 9/15），
但推不上去，于是线上一直停在 9/11。

---

## 二、关键事实对照

| 对象 | 数据最新日期 | 数据文件时间 | daemon 状态 | 与远程关系 |
|---|---|---|---|---|
| 源数据目录 `E:\Work\Data\订单\出票总订单数据` | **2026-09-15** | 9/15 08:30 | — | 正常，每天 08:30 落新文件 ✅ |
| D 盘副本 `D:\自动出票数据分析项目` | 2026-09-11 | `dashboard_data.json` 9/11 13:10 | **已死**（pid 26192/25248 进程不存在） | `origin/main` 同步点，**它就是推上去的那一份** |
| E 盘副本 `E:\Work\Projects\auto-ticket-dashboard\auto-ticket-dashboard` | **2026-09-15** | `dashboard_data.json` 9/15 18:44 | **存活**（pid 3468） | `ahead 6, behind 4` —— **分叉，push 被拒** |
| GitHub `origin/main` = `42b4d6c` | 2026-09-11 | — | — | 线上真正部署的那份 |

线上页面读取链路：

```
Streamlit Cloud → dashboard_v5.html（仓库里）
   → 内部 const COMMIT = "108d573"
   → jsDelivr: .../auto-ticket-dashboard@108d573/dashboard_data.json
   → 该文件 generated_at = 2026-09-11 13:10，9 月最后一天 = 9/11
```

---

## 三、异常清单（按严重度）

### 🔴 异常 1：git 推送自 9/11 11:37 起全部失败（这是网页停在 9/11 的直接原因）

`status.db` 里 push 失败记录（`error` 字段原文）：

```
git push 失败（commit 已保存到本地）:
 ! [rejected]  main -> main (fetch first)
error: failed to push some refs to 'github.com:yikedashua-create/auto-ticket-dashboard.git'
```

| 失败时间点 | 处理的文件 | 结果 |
|---|---|---|
| 2026-09-11 11:37:02 | 2026-09-10.xlsx | push 被拒 |
| 2026-09-14 08:37:02 | 2026-09-12.xlsx | push 被拒 |
| 2026-09-14 09:07:00 | 2026-09-12.xlsx | push 被拒 |
| 2026-09-15 09:37:01 | 2026-09-14.xlsx | push 被拒 |
| 2026-09-15 17:07:01 | 2026-09-14.xlsx | push 被拒 |
| 2026-09-15 18:37:01 | 2026-09-14.xlsx | push 被拒 |

9/12 之后共 **15 次失败**（其中 push 被拒为主因），113 次"成功"（多为无变更跳过 commit）。

**成因时间线：**

| 时间 | 事件 |
|---|---|
| 9/10 14:19 | E 盘最后**一次**成功推送 → `origin/main = 2e13c95`（两份副本的共同祖先） |
| 9/11 11:14 | **D 盘** daemon 启动，连推 `1c71fc9`、`1d8579f` → 远程进入 **D 那条线** |
| 9/11 11:37 | **E 盘** 兜底任务生成 `4ca075f` 并 push → **被拒**（远程已是 D 那条线） |
| 9/11 13:10 | D 盘再推 `108d573`、`42b4d6c` → `origin/main = 42b4d6c` |
| 9/11 11:17 | D 盘 daemon 处理 2026-09-10.xlsx **失败**（pandas 报错），之后再无动作 → **daemon 实际已死** |
| 9/12 起 | E 盘继续每天生成数据并 commit（本地 6 个 commit），但每次 push 都因分叉被拒 |

分叉示意图（共同祖先 `2e13c95` 之后各走各的）：

```
2e13c95 ──┬── 1c71fc9 ── 1d8579f ── 108d573 ── 42b4d6c   ← D 盘线（= 远程 origin/main，线上就是这份）
          └── 4ca075f ── 6042623 ── 5469c10 ── d6db569 ── 4230234 ── 5a14c14  ← E 盘线（有 9/12~9/15 数据，未上线）
```

### 🟠 异常 2：D 盘副本没有源码价值，但仍在抢远程历史

D 盘那 4 个独有 commit 只改了 `dashboard_data.json`、`monthly/2026-09.json`、
`dashboard_v5.html`，**没有任何 .py 源码改动**。`gen_dashboard_data.py` 两份 md5 完全一致
（`6185e6c9…`），所以合并时 D 侧没有需要保留的代码修复，只有一份已被 E 覆盖的旧数据。

### 🟠 异常 3：HTML 的 `COMMIT` 常量没跟着走（修好 push 也要一起改，否则前端还是旧数据）

| 文件 | `const COMMIT` | 对应数据 |
|---|---|---|
| D 盘 `dashboard_v5.html` | `108d573` | 9/11 |
| 远程（线上部署的） | `108d573` | 9/11 |
| E 盘 `dashboard_v5.html` | `d6a11e4` | **9/10** ← 比线上还旧 |

前端数据 URL 是 `jsdelivr…@${COMMIT}/dashboard_data.json`。**只修 push 不够**：必须把
`COMMIT` 重新指向新的数据 commit 并推送，否则浏览器仍然拉旧数据。

### 🟠 异常 4：30 分钟兜底任务在反复重跑同一个文件

计划任务 `auto_ticket_dashboard_sync_30min` 每 30 分钟执行一次
`python -m auto_sync trigger`，而 `trigger_now()` 会取监控目录里 **mtime 最新的 xlsx**。
由于源目录里 `2026-09-14.xlsx`（9/15 08:30:21）比 `2026-09-15.xlsx`（9/15 08:30:09）
**晚 12 秒**，兜底任务每次都选中 `2026-09-14.xlsx`，从 9/15 09:37 一直重跑到
9/16 08:20 —— 每 30 分钟烧掉约 7.5 分钟 CPU，且是下列崩溃的高频触发源。

| 时间 | 报错摘要 |
|---|---|
| 9/15 12:37 | `gen_dashboard_data.py 失败:`（错误信息为空） |
| 9/15 16:37 | pandas `_merge_blocks` / `np.vstack` 栈溢出类 |
| 9/15 17:37 | pandas `reshape/concat.py` `concatenate_managers` |
| 9/15 20:37 | `gen_dashboard_data.py 失败:`（错误信息为空） |

### 🟡 异常 5：D 盘的手工入口路径失效

`D:\自动出票数据分析项目\更新数据.bat` 里写的是
`cd /d "C:\Users\admin\Desktop\auto-ticket-dashboard"`，该目录 **已不存在**
（项目早已迁到 D 盘 / E 盘）。这个 bat 目前跑不到正确的项目目录。
另外 bat 用的是裸 `python`，不是 venv 里的
`D:\pycharm3\.venv\Scripts\python.exe`，容易命中缺依赖的解释器
（`start_auto_sync.bat` 已经备注过这个坑）。

---

## 四、修复方案（建议执行顺序）

**主副本选 E 盘**（数据最全到 9/15、daemon 存活、源目录一致）。

1. **确认 E 盘本地数据完整**：`raw/` 已到 `2026-09-15.parquet`，
   `dashboard_data.json` 的 9 月最后一天 = 9/15 ✅（已核实）
2. **合并远程**（把 E 盘 6 个 commit 重放到 `origin/main` 之上，消除分叉）：
   `git fetch origin` → `git reset --soft origin/main` → 重新提交为一个数据 commit
   （`dashboard_data.json` / `monthly/` 取 E 盘 9/15 版本；D 侧无源码改动可丢弃）
3. **推送**：`git push origin main`（rebase/reset 之后是 fast-forward，**不需要 force push**，
   远程这 4 个 commit 会作为历史保留）
4. **同步 HTML COMMIT 常量**到新的数据 commit → 再 commit + push
   （即把 E 盘 `dashboard_v5.html` 里的 `d6a11e4` 改成新的 40 位短 hash）
5. **验证**：刷新 `https://auto-ticket-dashboard.streamlit.app/`，
   确认 9 月最后一天变成 9/15；`git ls-remote origin refs/heads/main` 应等于 E 盘 HEAD
6. **善后（防复发）**
   - 停用 / 归档 **D 盘副本**（否则以后还会再分叉、再抢推）；`AGENTS.md` 里记的"项目根"
     与实际在跑的 E 盘并不一致，建议一并更新
   - 修正 `更新数据.bat` 的 cd 路径 + 改用 venv python
   - 兜底任务改为"处理未处理过的文件"而不是"取 mtime 最新"，并加"无变更则跳过 gen"

### 风险提示

- 步骤 2 会**重写 E 盘本地那 6 个 commit 的历史**（远程历史不动，不是 force push）。
  D 盘那 4 个 commit 里的数据会被 E 盘的 9/15 数据覆盖——这是期望行为，因为它们本来就是旧数据。
- 若希望线上尽快先出数据，最小操作是步骤 2+3+4。

---

## 五、附：本次诊断用到的核查命令

```bash
# 远程真实 HEAD
git ls-remote origin refs/heads/main            # → 42b4d6c（9/11）

# 两份副本各在什么位置
cd /d/自动出票数据分析项目 && git log --oneline -4 --date=iso
cd /e/Work/Projects/auto-ticket-dashboard/auto-ticket-dashboard && git log --oneline -8

# push 是否会被拒（只读预演）
cd /e/Work/Projects/auto-ticket-dashboard/auto-ticket-dashboard && git push --dry-run origin main
# → ! [rejected] main -> main (fetch first)

# 本地数据 vs 线上数据的最新日期
python -c "import json;d=json.load(open('dashboard_data.json',encoding='utf-8'));print(d['generated_at'], d['months']['2026-09']['daily'][-1]['date'])"
```
